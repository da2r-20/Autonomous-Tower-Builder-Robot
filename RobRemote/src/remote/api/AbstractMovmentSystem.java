package remote.api;

public interface AbstractMovmentSystem {

	/**
	 * this method handles the turning mechanism of the chassis
	 * its initiates the left in-place turning and 
	 * @return
	 */
	public boolean turnLeft();
		
}
